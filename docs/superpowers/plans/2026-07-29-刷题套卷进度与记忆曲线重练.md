# 刷真题：套卷进度 + 记忆曲线重练（A+C）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把刷真题模块改造为"按套卷组织 + 记录每题进度 + 记忆曲线自动重练"，支持四种练习模式与单题练习。

**Architecture:** 全部改动在前端 `static/app.js`（纯 JS，无构建）。用户 state 新增 `S.drillProg`（按题目 id 记录练习历史与记忆曲线 due），服务器 `server.py` 对 state 结构透明、无需改动。套卷列表/模式选择/重练队列均由 `QUESTIONS` + `S.drillProg` 在前端实时聚合渲染。复用现有 `.study-mod`/`.progress`/`.btn` 样式，新增极少量 CSS。

**Tech Stack:** 原生 JS + Python `http.server`（`server.py`）。无测试框架——验证靠 Node 跑纯函数单测 + 浏览器手动验证。

## Global Constraints

- 日期一律用现有 `todayStr()`（本地时区，`YYYY-MM-DD`），禁止 `new Date().toISOString()`。
- 记忆曲线复用现有常量 `REVIEW_IV = [1, 3, 7, 15]`（`static/app.js:12`）。
- 不修改 `questions.json`；题目唯一键用现有 `q.id`。
- `S.wrong` 错题本逻辑（`recordWrong`）保持不变，与 `drillProg` 并存。
- 所有新增用户可见文案用中文；UI 复用现有 `.study-mod`/`.progress`/`.task`/`.btn`/`.note` class。
- `drillProg` 中引用到已不在 `QUESTIONS` 里的 qid 时，统计与重练队列须跳过（防止题库更新后报错）。
- 每次 state 变更后仍走现有 `save()` 同步流程，不新增持久化通道。

---

### Task 1: 核心数据层 —— drillProg 记录与查询纯函数

**Files:**
- Modify: `static/app.js`（在 `ensurePlan()` 与"刷真题"区块之间插入；具体锚点见步骤）
- Test: `test_drill.js`（新建，Node 运行的纯函数单测）

**Interfaces:**
- Produces（后续任务依赖这些函数名与签名，全部挂为全局函数）:
  - `ensureDrillProg()` — 无参；确保 `S.drillProg` 为对象，返回它。
  - `recordDrill(qid, correct)` — 记录一次作答；更新 `n/ok/lastOk/lastAt/stage/due`。返回该题记录对象。
  - `drillStat(qid)` — 返回 `{n, ok, acc, lastOk, due, stage, cleared}`；无记录返回 `null`。`acc` 为 0-100 整数，`cleared` 为布尔（stage 已超 REVIEW_IV 即巩固）。
  - `dueDrills()` — 返回"今日待重练"题目对象数组（`due ≤ todayStr()` 且未 cleared 且题存在），按 `due` 升序。
  - `srcList()` — 返回套卷数组 `[{src, total, done, ok, acc, dueN}]`，按 `src` 排序。

- [ ] **Step 1: 写失败的 Node 单测（测真代码，不复制函数）**

新建 `test_drill.js`（Node 直接 `node test_drill.js` 运行，断言用 `assert`）。测试**不复制** app.js 的函数——而是读取 `static/app.js` 文本，定位"刷题纯函数块"（见 Step 3 的分隔注释标记），把这段真实现连同它依赖的纯函数（`todayStr/addDays/cmpDate/now`）一起提取出来，用 `new Function(依赖名..., 代码块 + 导出语句)` 构建后测试。这样测的就是线上跑的同一份代码。

提取机制（已验证可行）：用正则定位 `function 名(...) {`，再按花括号配平找到函数结束，截出源码段。依赖桩（`S`、`QUESTIONS`、`REVIEW_IV`、可覆写的 `TODAY`）作为 `new Function` 的参数注入。

```js
// test_drill.js
const assert = require('assert');
const fs = require('fs');
const SRC = fs.readFileSync('static/app.js', 'utf8');

// 按花括号配平提取某标识符定义段的源码（function 或整块注释区间）
function grabFn(name) {
  const re = new RegExp('function ' + name + '\\s*\\(([^)]*)\\)\\s*\\{');
  const m = re.exec(SRC);
  if (!m) throw new Error('未找到函数 ' + name);
  const start = src_braceStart(m.index);
  return SRC.slice(m.index, start.end + 1);
}
function src_braceStart(idx) {
  const start = SRC.indexOf('{', idx);
  let depth = 0;
  for (let j = start; j < SRC.length; j++) {
    if (SRC[j] === '{') depth++;
    if (SRC[j] === '}') { depth--; if (depth === 0) return { end: j }; }
  }
  throw new Error('花括号未配平');
}

// 依赖纯函数（真代码）
const depsCode = ['todayStr', 'addDays', 'cmpDate', 'now'].map(grabFn).join('\n');
// 刷题纯函数块（真代码，Step 3 里这五个函数连续排列）
const drillCode = ['ensureDrillProg', 'recordDrill', 'drillStat', 'dueDrills', 'srcList'].map(grabFn).join('\n');

// 用 new Function 注入可变依赖，构建被测模块。S/QUESTIONS/REVIEW_IV 注入；TODAY 用闭包内变量以便改写。
function build(S, QUESTIONS, TODAY) {
  const factory = new Function(
    'S', 'QUESTIONS', 'REVIEW_IV', 'TODAY',
    depsCode + '\n' +
    // 让 todayStr 用注入的 TODAY（覆盖真 todayStr）
    'function todayStr(){ return TODAY; }\n' +
    drillCode + '\n' +
    'return { ensureDrillProg, recordDrill, drillStat, dueDrills, srcList, addDays };'
  );
  return factory(S, QUESTIONS, [1, 3, 7, 15], TODAY);
}

const QUESTIONS = [
  { id: 'q1', src: '卷A', part: '词汇语法' },
  { id: 'q2', src: '卷A', part: '词汇语法' },
  { id: 'q3', src: '卷B', part: '阅读理解' },
];
const REVIEW_IV = [1, 3, 7, 15];
let TODAY = '2026-07-29';

// 每次用例重建，保证 S 隔离
function fresh() { const S = {}; const m = build(S, QUESTIONS, TODAY); m.S = S; return m; }

// 测试 1：首次作答答对
let m = fresh();
let r = m.recordDrill('q1', true);
assert.strictEqual(r.n, 1);
assert.strictEqual(r.ok, 1);
assert.strictEqual(r.lastOk, 1);
assert.strictEqual(r.stage, 1);
assert.strictEqual(r.due, m.addDays(TODAY, REVIEW_IV[1])); // stage=1 -> +3

// 测试 2：答错重置 stage、due=明天
r = m.recordDrill('q1', false);
assert.strictEqual(r.n, 2);
assert.strictEqual(r.ok, 1);
assert.strictEqual(r.lastOk, 0);
assert.strictEqual(r.stage, 0);
assert.strictEqual(r.due, m.addDays(TODAY, 1));

// 测试 3：drillStat 聚合
const st = m.drillStat('q1');
assert.strictEqual(st.n, 2);
assert.strictEqual(st.ok, 1);
assert.strictEqual(st.acc, 50);
assert.strictEqual(m.drillStat('q_nope'), null);

// 测试 4：dueDrills 只含 due<=今天且未巩固且题存在（明天该出现 q1）
m = build(m.S, QUESTIONS, m.addDays(TODAY, 1)); // 到明天
assert.ok(m.dueDrills().some(q => q.id === 'q1'));

// 测试 5：连对 REVIEW_IV.length 次后巩固，不再进 due
m = fresh();
for (let i = 0; i < REVIEW_IV.length; i++) m.recordDrill('q2', true);
assert.strictEqual(m.drillStat('q2').cleared, true);
assert.ok(!m.dueDrills().some(q => q.id === 'q2'));

// 测试 6：srcList 聚合套卷进度
m = fresh();
m.recordDrill('q1', true);
m.recordDrill('q2', false);
const a = m.srcList().find(s => s.src === '卷A');
assert.strictEqual(a.total, 2);
assert.strictEqual(a.done, 2);
assert.strictEqual(a.ok, 1);
assert.strictEqual(a.acc, 50);

console.log('全部通过');
```

- [ ] **Step 2: 运行确认失败**

Run: `node test_drill.js`
Expected: 抛 `未找到函数 ensureDrillProg`（因为 Step 3 还没在 app.js 实现它）。

- [ ] **Step 3: 在 app.js 实现 5 个函数**

在 `static/app.js` 中，把 `S.drillProg` 初始化加进 `ensurePlan()`（`static/app.js:458-466`），并在"刷真题"区块（`/* ---------- 刷真题 ---------- */`，约 `static/app.js:1303`）上方插入实现：

```js
// ensurePlan() 内追加一行：
if(!S.drillProg || typeof S.drillProg !== 'object') S.drillProg = {};

/* ---------- 刷题进度 / 记忆曲线 ---------- */
function ensureDrillProg(){
  if(!S.drillProg || typeof S.drillProg !== 'object') S.drillProg = {};
  return S.drillProg;
}

function recordDrill(qid, correct){
  const dp = ensureDrillProg();
  const td = todayStr();
  let r = dp[qid];
  if(!r) r = dp[qid] = { n: 0, ok: 0, lastOk: 0, lastAt: 0, stage: 0, due: '' };
  r.n++;
  if(correct) r.ok++;
  r.lastOk = correct ? 1 : 0;
  r.lastAt = now();
  if(correct){
    r.stage = (r.stage || 0) + 1;
    r.due = r.stage >= REVIEW_IV.length ? '' : addDays(td, REVIEW_IV[r.stage]);
  }else{
    r.stage = 0;
    r.due = addDays(td, 1);
  }
  return r;
}

function drillStat(qid){
  const r = (S.drillProg || {})[qid];
  if(!r) return null;
  return {
    n: r.n, ok: r.ok,
    acc: r.n ? Math.round(r.ok / r.n * 100) : 0,
    lastOk: r.lastOk, due: r.due || '', stage: r.stage || 0,
    cleared: (r.stage || 0) >= REVIEW_IV.length
  };
}

function dueDrills(){
  const td = todayStr();
  return QUESTIONS.filter(q => {
    const s = drillStat(q.id);
    return s && !s.cleared && s.due && s.due <= td;
  }).sort((a, b) => cmpDate(drillStat(a.id).due, drillStat(b.id).due));
}

function srcList(){
  const bySrc = {};
  QUESTIONS.forEach(q => { (bySrc[q.src] = bySrc[q.src] || []).push(q); });
  return Object.keys(bySrc).sort().map(src => {
    const qs = bySrc[src];
    let done = 0, ok = 0, dueN = 0;
    const td = todayStr();
    qs.forEach(q => {
      const s = drillStat(q.id);
      if(s){
        done++;
        ok += s.ok ? 1 : 0;
        if(!s.cleared && s.due && s.due <= td) dueN++;
      }
    });
    return { src, total: qs.length, done, ok, acc: done ? Math.round(ok / done * 100) : 0, dueN };
  });
}
```

- [ ] **Step 4: 运行真代码单测确认通过**

`test_drill.js` 直接从 app.js 提取实现来测，无需手动同步。确认这五个函数在 app.js 里**连续排列**（Step 3 已满足），提取器才能按名找到。

Run: `node test_drill.js`
Expected: 输出 `全部通过`。若报"未找到函数 X"，检查该函数名拼写与花括号配平。

- [ ] **Step 5: 浏览器手动冒烟**

启动 `server.py`，打开 `http://localhost:5000`，选用户进首页，控制台执行 `srcList()`，确认返回数组且无报错。

- [ ] **Step 6: Commit**

```bash
git add static/app.js test_drill.js
git commit -m "feat(drill): 新增 drillProg 数据层与记忆曲线纯函数"
```

---

### Task 2: 套卷列表首页（renderDrillHome 重写）

**Files:**
- Modify: `static/app.js`（重写 `renderDrillHome`，约 `1306-1330`）
- Modify: `static/app.css`（追加套卷卡片/模式按钮少量样式）

**Interfaces:**
- Consumes: Task 1 的 `srcList()`、`dueDrills()`、`drillStat()`、`ensureDrillProg()`。
- Produces:
  - `renderDrillHome()` — 渲染套卷列表（覆盖原函数，签名不变，被 `wireNav`/`reRenderActive` 调用）。
  - `openDrillSrc(src)` — 展开某套卷的模式选择视图（写回 `#drillBox`）。
  - `renderDrillDue()` 由 `dueDrills()` 驱动的一键重练入口按钮（在首页顶部）。

- [ ] **Step 1: 重写 renderDrillHome 为套卷列表**

替换原 `renderDrillHome`（`static/app.js:1306-1330`）整体。顶部放"今日待重练"卡，下面按 `srcList()` 渲染套卷卡，底部保留"全部混合"与"按题型"入口：

```js
function renderDrillHome(){
  const due = dueDrills();
  const srcs = srcList();
  const parts = [...new Set(QUESTIONS.map(q => q.part))];
  let html = `<h2>刷真题（${QUESTIONS.length} 题）</h2>`;
  html += `<div class="study-mod" onclick="startDueDrill()">
    <div><div class="sm-title">今日待重练</div>
    <div class="sm-meta">按记忆曲线该重做的题</div></div>
    <div class="sm-prog"><div class="num" style="font-size:22px;color:#8b5e3c">${due.length}</div>
    <div class="note">${due.length ? '去重练 ›' : '无'}</div></div></div>`;
  html += `<h2 style="font-size:15px;margin-top:14px">按套卷</h2>`;
  srcs.forEach(s => {
    const pct = s.total ? Math.round(s.done / s.total * 100) : 0;
    html += `<div class="study-mod" onclick="openDrillSrc('${encodeURIComponent(s.src)}')">
      <div><div class="sm-title">${esc(s.src)}</div>
      <div class="sm-meta">${s.total} 题 · 已做 ${s.done} · 正确率 ${s.acc}%${s.dueN ? ' · <span style="color:#e57373">待重练 ' + s.dueN + '</span>' : ''}</div></div>
      <div class="sm-prog"><div class="note">${s.done}/${s.total}</div>
      <div class="progress"><div style="width:${pct}%"></div></div></div></div>`;
  });
  html += `<h2 style="font-size:15px;margin-top:14px">按题型</h2>`;
  parts.forEach(p => {
    const qs = QUESTIONS.filter(q => q.part === p);
    html += `<div class="task"><div>${esc(p)}<div class="note">${qs.length} 题</div></div>
      <button class="btn" onclick="startDrill('${encodeURIComponent(p)}')">开始</button></div>`;
  });
  html += `<div class="task"><div>全部混合练习</div><button class="btn" onclick="startDrill('')">开始</button></div>`;
  document.getElementById('drillBox').innerHTML = html;
}
```

注意：原 `startDrill(part)` 接收的是 part 名，本任务给它传 `encodeURIComponent(p)`，需在 Task 3 让 `startDrill` 内部 `decodeURIComponent`。此处保持现状即可（part 名多为中文，旧代码用的是 `startDrillSrc(encodeURIComponent(s))` 模式）。为避免编码不一致，本任务统一改为传已编码值，并在 Task 3 的 `startDrill`/`startDrillSrc` 里统一 decode。

- [ ] **Step 2: 实现 openDrillSrc 模式选择视图**

在 `renderDrillHome` 后新增：

```js
function openDrillSrc(enc){
  const src = decodeURIComponent(enc);
  const qs = QUESTIONS.filter(q => q.src === src);
  const notDone = qs.filter(q => !drillStat(q.id)).length;
  const wrongN = qs.filter(q => { const s = drillStat(q.id); return s && s.ok < s.n; }).length;
  let html = `<h2>${esc(src)}</h2>
    <p class="note" style="margin-bottom:12px">
      <a href="javascript:renderDrillHome()" style="color:#8b5e3c">‹ 返回套卷列表</a>
      　共 ${qs.length} 题 · 未做 ${notDone} · 曾错 ${wrongN}</p>
    <div class="task"><div>顺序做<div class="note">按原题号从第 1 题开始</div></div>
      <button class="btn" onclick="startDrillSeq('${enc}')">开始</button></div>
    <div class="task"><div>只做没做过的<div class="note">${notDone} 题</div></div>
      <button class="btn" onclick="startDrillNew('${enc}')">开始</button></div>
    <div class="task"><div>只做做错的<div class="note">${wrongN} 题</div></div>
      <button class="btn" onclick="startDrillWrong('${enc}')">开始</button></div>
    <div class="task"><div>重做整套<div class="note">从头再过一遍（保留历史）</div></div>
      <button class="btn" onclick="startDrillSeq('${enc}')">开始</button></div>
    <h2 style="font-size:15px;margin-top:14px">题目列表（点「练」单题练习）</h2>`;
  qs.forEach((q, i) => {
    const s = drillStat(q.id);
    const badge = !s ? '<span class="note">未做</span>'
      : s.cleared ? '<span style="color:#4caf50">已巩固</span>'
      : (s.lastOk ? '<span style="color:#4caf50">对</span>' : '<span style="color:#e57373">错</span>');
    html += `<div class="task"><div style="flex:1">第 ${i + 1} 题 <span class="note">${esc(q.part)}</span> ${badge}</div>
      <button class="btn ghost" onclick="startSingleDrill('${esc(q.id)}')">练</button></div>`;
  });
  document.getElementById('drillBox').innerHTML = html;
}
```

- [ ] **Step 3: 追加 CSS**

`static/app.css` 末尾追加（`.num` 复用，套卷卡复用 `.study-mod`，仅补少量）：

```css
/* 刷真题套卷卡片徽标与今日待重练 */
.study-mod .num{font-size:22px;font-weight:700;color:#8b5e3c}
.task .note{color:#999;font-size:12px;font-weight:400}
```

- [ ] **Step 4: 浏览器验证**

刷新页面进"刷真题"：看到顶部"今日待重练"卡 + 13 个套卷卡（各带进度条与正确率）+ 按题型 + 全部混合。点任一套卷出现四种模式 + 题目列表（每题带"练"按钮与状态徽标）。此步模式按钮暂不生效属正常（Task 3 实现）。

- [ ] **Step 5: Commit**

```bash
git add static/app.js static/app.css
git commit -m "feat(drill): 套卷列表首页与模式选择视图"
```

---

### Task 3: 练习模式启动 + 单题练习 + 答题时写 drillProg

**Files:**
- Modify: `static/app.js`（`startDrill`/`startDrillSrc` 区，约 `1332-1356`；`answerDrill` 约 `1371-1392`；新增模式启动函数）

**Interfaces:**
- Consumes: Task 1 `recordDrill()`、`drillStat()`、`dueDrills()`；Task 2 `renderDrillHome()`、`openDrillSrc()`。
- Produces:
  - `startDrillSeq(enc)` / `startDrillNew(enc)` / `startDrillWrong(enc)` — 套卷三种模式启动（enc 为 encodeURIComponent 后的 src）。
  - `startDueDrill()` — 今日待重练启动。
  - `startSingleDrill(qid)` — 单题练习。
  - 修改后的 `answerDrill()` — 在原逻辑基础上调用 `recordDrill()`。

- [ ] **Step 1: 统一 startDrill / startDrillSrc 解码**

把 `startDrill(part)`（`static/app.js:1332`）首行改为解码，兼容 Task 2 传入的编码值与旧调用：

```js
function startDrill(part){
  if(!requireReady()) return;
  part = part ? decodeURIComponent(part) : '';
  drillMode = part;
  drillQ = part ? QUESTIONS.filter(q => q.part === part) : QUESTIONS.slice();
  shuffle(drillQ);
  drillI = 0; drillScore = 0; drillWrong = [];
  if(!drillQ.length){ alert('题库为空'); return; }
  showDrillQ();
}
```

`startDrillSrc` 保持不变（已 decode），但其入口已不在首页，可保留供其它入口使用。

- [ ] **Step 2: 实现四种启动 + 单题 + 重练**

在 `startDrillSrc` 后新增。复用一个内部装填函数 `_beginDrill(list, mode)`：

```js
function _beginDrill(list, mode, ordered){
  if(!requireReady()) return;
  drillMode = mode;
  drillQ = list.slice();
  if(!ordered) shuffle(drillQ);
  drillI = 0; drillScore = 0; drillWrong = [];
  if(!drillQ.length){ alert('该模式下暂无题目'); renderDrillHome(); return; }
  showDrillQ();
}

function startDrillSeq(enc){
  const src = decodeURIComponent(enc);
  _beginDrill(QUESTIONS.filter(q => q.src === src), 'seq:' + src, true);
}
function startDrillNew(enc){
  const src = decodeURIComponent(enc);
  _beginDrill(QUESTIONS.filter(q => q.src === src && !drillStat(q.id)), 'new:' + src, false);
}
function startDrillWrong(enc){
  const src = decodeURIComponent(enc);
  _beginDrill(QUESTIONS.filter(q => { const s = drillStat(q.id); return q.src === src && s && s.ok < s.n; }), 'wrong2:' + src, false);
}
function startDueDrill(){
  if(!requireReady()) return;
  const due = dueDrills();
  if(!due.length){ alert('今天没有待重练的题'); return; }
  _beginDrill(due, 'due', true);
}
function startSingleDrill(qid){
  if(!requireReady()) return;
  const q = QUESTIONS.find(x => x.id === qid);
  if(!q){ alert('题目不存在'); return; }
  _beginDrill([q], 'single', true);
}
```

- [ ] **Step 3: answerDrill 写入 drillProg**

在 `answerDrill`（`static/app.js:1371`）中，`recordWrong(q, chosen, correct);` 这一行（约 `1383`）下方紧邻处插入一行：

```js
  recordWrong(q, chosen, correct);
  recordDrill(q.id, correct);   // 新增：进度 + 记忆曲线
```

- [ ] **Step 4: 完成后返回逻辑适配**

`nextDrill`（约 `1394`）的结束页"再练"按钮，单题/重练模式应返回合适页面。修改结束页渲染，依据 `drillMode` 决定返回目标：

```js
function nextDrill(){
  drillI++;
  if(drillI < drillQ.length){ showDrillQ(); return; }
  const m = drillMode;
  let back;
  if(m === 'wrong') back = `goto('wrong')`;
  else if(m === 'single') back = 'renderDrillHome()';
  else if(m === 'due') back = 'renderDrillHome()';
  else if(m.indexOf('seq:') === 0 || m.indexOf('new:') === 0 || m.indexOf('wrong2:') === 0)
    back = `openDrillSrc('${encodeURIComponent(m.split(':').slice(1).join(':'))}')`;
  else back = 'renderDrillHome()';
  const backLabel = m === 'wrong' ? '回错题本' : '返回';
  document.getElementById('drillBox').innerHTML = `
    <h2>练习完成</h2>
    <p class="center" style="font-size:40px;margin:16px 0">${drillScore} / ${drillQ.length}</p>
    <p class="center note">${drillWrong.length ? '错 ' + drillWrong.length + ' 题，已存入错题本' : '全对！太棒了 🎉'}</p>
    <div class="row"><button class="btn" onclick="${back}">${backLabel}</button>
    <button class="btn ghost" onclick="renderDrillHome()">套卷列表</button></div>`;
}
```

注意：原 `nextDrill` 里 `backWrong` 逻辑被上面的 `m === 'wrong'` 分支取代；`startWrongReview`（错题本，置 `drillMode='wrong'`）路径保持不变。

- [ ] **Step 5: 浏览器验证四种模式**

进某套卷：
- 顺序做：从第 1 题按顺序出题；
- 只做没做过的：答几题后重进，已做的不再出现；
- 只做做错的：故意答错一题后，该模式包含它；
- 单题"练"：只出该题，答完返回。
答一题后控制台 `drillStat(该题id)` 有记录，`dueDrills()` 在该题答错时含它。

- [ ] **Step 6: Commit**

```bash
git add static/app.js
git commit -m "feat(drill): 四种练习模式/单题练习/答题写入记忆曲线"
```

---

### Task 4: 错题本"重练此题"按钮 + 今日页待重练数

**Files:**
- Modify: `static/app.js`（`renderWrong` 约 `1437-1460`；`renderHome` 任务行约 `727-733`）

**Interfaces:**
- Consumes: Task 3 `startSingleDrill()`；Task 1 `dueDrills()`。
- Produces: 无新函数（仅改渲染）。

- [ ] **Step 1: 错题本每题加"重练此题"**

`renderWrong` 中错题条目（约 `1453-1456`），在"看题"按钮旁加：

```js
    <button class="btn ghost" onclick="previewWrong('${esc(x.q.id)}')">看题</button>
    <button class="btn ghost" onclick="startSingleDrill('${esc(x.q.id)}')">重练</button></div>`);
```

（注意保留原有 `.join('')` 结构与闭合。）

- [ ] **Step 2: 今日页"刷真题"行显示待重练数**

`renderHome` 的 tasks 数组（约 `731`），把刷真题行的 `prog` 改为含待重练数：

```js
    { name: '刷真题', prog: (() => { const dn = dueDrills().length; return `题库 ${QUESTIONS.length} 题` + (dn ? ` · 待重练 ${dn}` : ''); })(), go: 'drill', btn: '去刷题' },
```

- [ ] **Step 3: 浏览器验证**

错题本每题出现"重练"按钮并可单题重练；今日页"刷真题"行在有 due 题时显示"待重练 N"。

- [ ] **Step 4: Commit**

```bash
git add static/app.js
git commit -m "feat(drill): 错题本重练按钮与今日页待重练提示"
```

---

### Task 5: 回归验证 + 兼容性测试

**Files:**
- Test: `test_drill.js`
- Modify: `static/app.js`（若发现问题）

**Interfaces:**
- Consumes: 全部前序任务。

- [ ] **Step 1: 跑 Node 单测**

Run: `node test_drill.js`
Expected: `全部通过`。

- [ ] **Step 2: 老用户兼容**

用已存在用户（`users/` 目录有数据的用户，其 state 无 `drillProg`）登录，进"刷真题"：进度显示 0、无报错；做一题后进度出现。控制台 `S.drillProg` 已被初始化。

- [ ] **Step 3: 记忆曲线跨天**

控制台模拟：对某题 `recordDrill('某id', false)`，确认 `due` = 明天；把系统日期相关的 `todayStr` 无法在浏览器改，则改 `S.drillProg[该id].due = todayStr()` 后 `dueDrills()` 应含该题，且"今日待重练"卡数 +1。

- [ ] **Step 4: 手机 App 端冒烟**

用局域网地址在手机浏览器打开，重复 Task 3 Step 5 的关键路径，确认触屏下单题/模式可用、进度同步（顶部同步 chip 显示"已同步"）。

- [ ] **Step 5: 清理临时单测（可选保留）**

若团队不需要把 `test_drill.js` 留在仓库，可移入 `.gitignore` 或删除；否则保留。确认后：

```bash
git add -A
git commit -m "test(drill): 记忆曲线纯函数单测与回归" || echo "无改动"
```

---

## Self-Review 记录

- **Spec 覆盖**：§2 数据结构→Task1；§3 套卷列表→Task2；§4 记忆曲线重练→Task1(dueDrills)+Task3(startDueDrill)+Task4(home 提示)；§5 单题按钮→Task2(题目列表"练")+Task4(错题本"重练")；§7 边界（qid 失效、空模式、due 空/未来）→Task1(dueDrills/drillStat 判空)+Task3(_beginDrill 空列表提示)。无缺口。
- **占位符**：无 TBD/TODO；所有代码步骤含完整可运行代码。
- **类型一致**：`ensureDrillProg/recordDrill/drillStat/dueDrills/srcList`（Task1 定义）在 Task2/3/4 引用一致；`startDrillSeq/startDrillNew/startDrillWrong/startDueDrill/startSingleDrill/_beginDrill`（Task3 定义）与 Task2 `openDrillSrc` 中的 `onclick` 调用一致；`openDrillSrc`/`renderDrillHome`（Task2 定义）在 Task3 引用一致。
